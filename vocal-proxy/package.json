{
  "name": "vocal-proxy-root",
  "version": "1.0.0",
  "scripts": {
    "setup": "node setup-assistant.js",
    "dev": "cd app && npm run dev",
    "install:all": "npm install && cd app && npm install"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.0"
  }
}
